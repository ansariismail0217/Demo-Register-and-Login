#spring.datasource.url = jdbc:mysql://localhost:3306/login_system?useSSL=false
spring.datasource.url=jdbc:mysql://localhost:3306/loginapi?createDatabaseIfNotExist=true&useSSL=false
spring.datasource.username = root
spring.datasource.password = abdul@mysql1


## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQLDialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update

logging.level.org.springframework.security=DEBUG

# to get formatted json response in browser
spring.jackson.serialization.INDENT_OUTPUT=true